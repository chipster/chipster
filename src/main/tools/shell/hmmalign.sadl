#/v/linux26_x86_64/appl/molbio/hmmer/hmmer-3.0/bin/hmmalign <hmmfile> <seqfile>
TOOL "HMM" / hmmalign.sadl: hmmalign ( align sequences to a profile HMM)
INPUT hmmfile-value-only: "HMM file" TYPE GENERIC
INPUT seqfile-value-only: "sequence file" TYPE GENERIC
OUTPUT o: out.txt
PARAMETER OPTIONAL  -outformat: "Output format" TYPE [ Stockholm: " Stockholm", Pfam: "Pfam", A2M: "A2M", PSIBLAST: "PSIBLAST"  ] DEFAULT Stockholm (Choose output format)
PARAMETER OPTIONAL trim-value-only: "trim nonaligned tails" TYPE [  : "No", --trim: "Yes"] (trim terminal tails of nonaligned residues from alignment)
PARAMETER OPTIONAL allcol-value-only: "include all consensus columns" TYPE [  : "No", --allcol: "Yes"] (include all consensus columns in alignment, even if all characters are gaps)